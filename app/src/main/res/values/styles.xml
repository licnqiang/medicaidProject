<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- 自定义Loading Dialog -->
    <style name="loading_dialog_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@color/transparent</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowContentOverlay">@null</item>
    </style>

    <!--<style name="MyTablayoutstyle" parent="TextAppearance.Design.Tab">-->
        <!--<item name="tabBackground">@color/white</item>-->
        <!--<item name="tabIndicatorColor">@color/them_color</item>-->
        <!--<item name="tabIndicatorHeight">2dp</item>-->
        <!--<item name="tabSelectedTextColor">@color/them_color</item>-->
        <!--<item name="android:textSize">15sp</item>-->
        <!--<item name="android:textColor">@color/them_black_font</item>-->
    <!--</style>-->

    <!--TabLayout字体样式-->
    <style name="TabTextStyle" parent="TextAppearance.Design.Tab">
        <!--Tab字体的大小-->
        <item name="android:textSize">15sp</item>
        <!--Tab字体的默认的颜色-->
        <item name="android:textColor">@color/them_color</item>
        <!--Tab字体是否为大写-->
        <item name="textAllCaps">true</item>
        <item name="android:background">@color/white</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="StyleProgressBar" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:maxHeight">50dip</item>
        <item name="android:minHeight">10dip</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">
            @android:drawable/progress_indeterminate_horizontal
        </item>
        <item name="android:progressDrawable">@drawable/progress_drawable</item>
    </style>


</resources>
